name: Link Check

on:
  schedule:
    - cron: '0 3 * * 0'
  workflow_dispatch:

jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 检查所有链接
        run: bash scripts/check-links.sh
